---
title: "Experience Funnel: A State-Policy Alternating Loop for Self-Evolving Agents"
title_zh: 经验漏斗：面向自演化智能体的状态-策略交替循环
authors: "Wenbo Gao, Zhaomou Song, Zhiyuan Ji, Renxi Liu, Xing Li, Xianzhi Yu, Xiaoguang Li, James Chung-wai Cheung, Weizhe Lin, Yaoyuan Wang"
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.08919v1"
tags: ["query:self-evolve"]
score: 9.0
evidence: 耦合快速文本状态适应与参数化策略更新的自进化框架
tldr: LLM智能体在交互中不断积累经验，但如何把任务特定经验转化为可复用能力、同时保留快速适应新证据的能力，仍是难题。本文提出Experience Funnel，用快状态适应与慢策略固化的交替循环实现自进化：轨迹先蒸馏为可编辑文本状态，再以转移感知蒸馏把跨状态修订仍有效的行为固化进策略。更新后的状态—策略对生成新轨迹，供下一轮迭代。在多类智能体基准上，该方法持续优于仅状态进化与策略内化方案，逐步将显式经验转化为自主策略能力。
source: arxiv
selection_source: fresh_fetch
motivation: 显式文本状态适应快却依赖外部上下文，参数化策略能力可复用但更新缓慢，二者难以兼得。
method: 交替循环：轨迹先蒸馏为文本状态，再用转移感知蒸馏筛选跨状态修订仍有效的行为并固化进策略，并生成新轨迹。
result: 在多样智能体基准上一致优于仅状态进化与策略内化方法，并逐步将显式经验转化为自主策略能力。
conclusion: 经验漏斗让快适应与慢固化互补，智能体在保留可编辑状态的同时持续积累可复用的自主能力。
---

## 摘要
由大型语言模型（LLMs）驱动的自主智能体通过交互不断积累经验，从而为通过自演化改进未来行为创造了机会。一个根本性挑战是，如何将丰富的、任务特定的交互经验转化为可复用的模型能力，同时不牺牲对新观察到证据快速适应的能力。显式文本状态（如技能和智能体支架）提供快速、人类可读且可编辑的适应，但会持续依赖外部上下文；参数化策略提供紧凑且可复用的能力，但更新速度要慢得多。我们提出经验漏斗，一种自演化框架，在交替循环中将快速的状态适应与缓慢的策略巩固耦合起来。交互轨迹首先被蒸馏为显式文本状态，新获得的经验可以在其中被快速纳入并验证。该框架随后有选择地识别在状态修订间仍然有用的、由状态使能的行为，并通过转移感知蒸馏将其巩固到策略中。更新后的状态—策略对随后生成新的推演轨迹，为下一轮状态适应和策略巩固提供新证据。在不同智能体基准上的实验表明，经验漏斗持续优于仅状态演化和策略内化方法，同时逐步将有用的显式经验转化为自主的策略能力。

## Abstract
Autonomous agents powered by large language models (LLMs) continuously accumulate experience through interaction, creating an opportunity to improve future behavior through self-evolution. A fundamental challenge is how to transform abundant, task-specific interaction experience into reusable model competence without sacrificing the ability to adapt rapidly to newly observed evidence. Explicit textual states, such as skills and agent harnesses, provide fast, human-readable and editable adaptation, but incur persistent dependence on external context; parametric policies provide compact and reusable competence, but are substantially slower to update. We present \textit{Experience Funnel}, a self-evolving framework that couples fast state adaptation with slow policy consolidation in an alternating loop. Interaction trajectories are first distilled into an explicit textual state, where newly acquired experience can be rapidly incorporated and validated. The framework then selectively identifies state-enabled behavior that remains useful across state revisions and consolidates it into the policy through transition-aware distillation. The updated state--policy pair subsequently generates new rollouts, providing fresh evidence for the next round of state adaptation and policy consolidation. Experiments across diverse agent benchmarks show that \textit{Experience Funnel} consistently improves agent capability over state-only evolution and policy-internalization approaches, while progressively converting useful explicit experience into autonomous policy competence.

---

## 论文详细总结（自动生成）

# 论文总结：Experience Funnel / 经验漏斗

## 1. 核心问题与整体含义
- **研究背景**：基于 LLM 的自主智能体可在反复交互中积累经验，但关键问题不是“积累更多经验”，而是如何把大量任务特定轨迹转化为可复用、可自主执行的模型能力，同时保留对新证据快速适应的能力。
- **核心矛盾**：
  - 显式文本状态（记忆、技能、流程指令、agent harness）适应快、可读可编辑，但持续依赖外部上下文，规模增长后带来检索/上下文成本、冗余与冲突。
  - 参数化策略能力紧凑、可复用、部署时无需外部提示，但更新慢；若直接把所有经验写入参数，可能固化噪声、样本特例或模型已掌握行为。
  - 显式经验的价值依赖当前策略：策略改进后，原有指导可能变得冗余、互补甚至冲突。
- **整体含义**：论文主张以“经验为中心”的自演化视角，将快速显式适应与慢速参数固化耦合起来，形成状态—策略交替循环；不是二选一，而是让经验在两种表示之间逐步迁移、筛选和保留。

## 2. 方法论
### 2.1 核心思想
- 提出 **Experience Funnel / 经验漏斗**，包含 **State–Policy Alternating Loop**：交互轨迹先被蒸馏为可编辑文本状态，再通过 **Transition-Aware Skill Distillation** 有选择地把跨状态修订仍有效的行为固化进策略。
- 更新后的状态—策略对重新部署，生成新轨迹，为下一轮状态适应和策略巩固提供证据。
- 形式化循环：\((S_k,\pi_k)\rightarrow S_{k+1}\rightarrow \pi_{k+1}\rightarrow S_{k+2}\rightarrow \pi_{k+2}\rightarrow\cdots\)。

### 2.2 状态演化
- 当前状态—策略对部署后收集交互轨迹，冻结为 reflection batch。
- 跨轨迹反思，而非单例纠错，提炼可复用流程、常见失败模式和纠正策略，形成候选状态编辑。
- 候选更新在独立 held-out 验证集上、由当前策略评估；只有提升环境级结果才接受，否则保留旧状态并跳过巩固。
- 接受后的显式状态是“中间经验表示”：既可快速适应，又为后续策略固化暴露可复用行为知识。

### 2.3 转移感知技能蒸馏
- **轨迹级选择**：对每个实例 \(x_i\)，评估当前策略 \(\pi_k\) 在无状态、旧状态 \(S_k\)、新状态 \(S_{k+1}\) 下的结果：
  - \(r_i^0=R(\pi_k,x_i)\)，\(r_i^-=R(\pi_k,x_i,S_k)\)，\(r_i^+=R(\pi_k,x_i,S_{k+1})\)。
  - 定义相对无状态执行的效用：\(b_i^-=I[r_i^->r_i^0]\)，\(b_i^+=I
